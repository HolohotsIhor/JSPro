function Room(number, students, tables, chairs) {
    this.number = number;
    this.students = students;
    this.tables = tables;
    this.chairs = chairs;

    this.showInfo = function()  {
        document.write(`div`);
    }
}
